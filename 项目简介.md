---

# StarC 运行时库完整报告

## 项目简介

StarC 运行时库是一套 **纯 C 语言编写的跨平台系统工具库**，共 **96 个模块**，涵盖文件操作、进程管理、网络通信、系统监控、加密解密、目录同步、日志处理、缓存服务、权限管理等领域。所有模块 **不依赖任何第三方库**，完全自研，可在 Windows 和 Linux (MSYS2) 环境下编译运行。

**项目目标**：为 StarC 自研编程语言提供完整的系统级运行时支持，使 StarC 程序能够直接调用底层系统功能。

---

## 📚 模块清单（共 96 个）

---

### 1. `rename_files` — 批量重命名文件

**功能简介**：批量重命名当前目录下的文件，支持前缀添加、后缀添加、字符串替换、序号重命名、大小写转换、去除空格等 6 种模式。支持递归子目录、文件过滤、试运行预览。

**亮点总结**：
1. 六种重命名模式覆盖所有常见批量重命名场景
2. 支持递归子目录处理，可一次性整理整个项目结构
3. 内置 dry-run 预览模式，防止误操作
4. 自动检测文件名冲突并提供覆盖选项
5. 可配置编号位数和起始序号，灵活适配不同格式

**代替产品**：rename (Linux)、PowerShell Rename-Item、Bulk Rename Utility

---

### 2. `count_lines` — 统计文件总行数

**功能简介**：递归统计目录下所有文本文件的总行数，自动处理文件末尾无换行符的情况，支持扩展名过滤。

**亮点总结**：
1. 逐行流式读取，支持超大文件
2. 自动处理文件末尾无换行符的边缘情况
3. 递归遍历子目录，累计总行数
4. 极简实现，无任何外部依赖，代码量仅 100 行

**代替产品**：wc -l (Linux)、find + wc、cloc

---

### 3. `is_prime` — 质数检测

**功能简介**：检测整数是否为质数，使用 6k±1 优化算法。支持质数范围查找、相邻质数计算、质因数分解。

**亮点总结**：
1. 6k±1 优化算法，时间复杂度 O(√n)
2. 内置小质数缓存，加速重复检测
3. 支持质数范围查找（如 1-1000 内所有质数）
4. 支持质因数分解，输出完整因子表达式
5. 1-999 范围内质数统计

**代替产品**：prime (Linux)、factor、Math::Prime::Util

---

### 4. `get_folder_size` — 计算目录总占用空间

**功能简介**：递归计算目录总大小（字节），记录最大文件及其路径，支持深度限制。

**亮点总结**：
1. 递归遍历所有子目录，计算准确总大小
2. 记录最大文件及其完整路径，便于快速定位大文件
3. 支持深度限制，防止无限递归
4. 格式化输出（B/KB/MB/GB/TB）
5. 统计文件数、目录数、错误数

**代替产品**：du (Linux)、GetFolderSize、ncdu

---

### 5. `generate_password` — 生成随机安全密码

**功能简介**：生成高强度随机密码，支持小写/大写/数字/符号四种字符集，支持排除模糊字符（0OIl1B8G6），支持每种类型至少一个。

**亮点总结**：
1. 使用 `/dev/urandom`（Linux）或 `CryptGenRandom`（Windows）真随机源
2. 四字符集自由组合，满足不同强度需求
3. 自动排除模糊字符，提高可读性
4. 强制每种类型至少一个，确保复杂度
5. 密码强度评分（极弱/弱/中等/强/极强）

**代替产品**：openssl rand、pwgen、KeePass 密码生成器

---

### 6. `search_in_files` — 递归搜索文件内容

**功能简介**：在指定目录下递归搜索文件中的文本内容，支持区分/不区分大小写、排除/包含扩展名、显示行号。

**亮点总结**：
1. 支持区分/不区分大小写两种搜索模式
2. 支持排除和包含扩展名，精准控制搜索范围
3. 显示匹配行号和行内容，定位准确
4. 自动跳过超大文件（>100MB），防止内存溢出
5. 统计匹配次数，便于评估结果规模

**代替产品**：grep (Linux)、findstr (Windows)、ack

---

### 7. `auto_backup` — 定时自动备份目录

**功能简介**：定时自动备份目录，支持增量备份（跳过未修改文件），带时间戳的备份目录命名，自动清理旧备份。

**亮点总结**：
1. 增量备份：只复制修改过的文件，大幅节省时间和存储空间
2. 时间戳命名的备份目录（精确到秒），便于追溯
3. 自动清理旧备份，保留最近 N 个版本
4. 备份日志记录（源目录/文件数/时间戳）
5. 支持排除模式和扩展名过滤

**代替产品**：rsync、Duplicity、BorgBackup

---

### 8. `count_files` — 统计目录内文件总数

**功能简介**：递归统计目录下的文件、目录、符号链接、隐藏文件、可执行文件、空文件数量，按扩展名分类统计。

**亮点总结**：
1. 区分文件/目录/符号链接/隐藏文件四种类型
2. 统计可执行文件和空文件数量
3. 按扩展名分类统计，一目了然
4. 记录最大/最小文件及其路径
5. 支持深度限制和排除目录

**代替产品**：find + wc -l、tree、FileCount

---

### 9. `detect_encoding` — 检测文件字符编码

**功能简介**：自动检测文本文件的字符编码，支持 17 种编码（UTF-8/UTF-16/GBK/BIG5/Shift-JIS 等），BOM 检测优先。

**亮点总结**：
1. 检测 17 种常见编码，覆盖主流字符集
2. BOM 检测优先，准确识别带 BOM 的编码
3. UTF-8 有效性校验，计算置信度
4. GBK/GB2312 双字节检测，支持中文编码
5. 置信度评分系统，辅助判断

**代替产品**：chardet、enca、file -i (Linux)

---

### 10. `rename_with_numbers` — 按序号批量重命名

**功能简介**：将目录中的文件按序号批量重命名，支持自定义命名模板（如 `file_%04d.txt`），支持按名称/日期/大小三种排序方式。

**亮点总结**：
1. 自定义命名模板（如 `file_%04d.txt`、`img_###.jpg`）
2. 三种排序方式：名称/日期/大小，灵活控制重命名顺序
3. 支持保留原扩展名或统一指定扩展名
4. 支持步长控制和起始序号
5. 递归子目录支持

**代替产品**：rename (Linux)、PowerShell、Advanced Renamer

---

### 11. `merge_text_files` — 合并多个文本文件

**功能简介**：将目录下多个文本文件按规则合并为一个文件，支持通配符模式匹配、递归子目录、添加文件名头、行号、分隔符。

**亮点总结**：
1. 通配符模式匹配（如 `*.txt`），灵活选择文件
2. 递归子目录，合并整个项目树
3. 可选添加文件名头、行号、分隔符
4. 支持过滤空行和排除模式
5. 生成带时间戳的合并报告

**代替产品**：cat、paste、awk

---

### 12. `watch_folder` — 实时监控文件夹变化

**功能简介**：监控文件夹内的文件创建/删除/修改/重命名等事件，支持回调通知。

**亮点总结**：
1. Linux：使用 `inotify` API 实时监控
2. Windows：使用 `ReadDirectoryChangesW`
3. 统一回调接口，方便集成到其他应用
4. 支持递归监控和事件过滤
5. 线程安全的事件分发

**代替产品**：inotifywait、Watchdog (Python)、fswatch

---

### 13. `csv_to_json` — CSV 格式转换为 JSON

**功能简介**：将 CSV 文件转换为 JSON 格式，自动检测分隔符（逗号/分号/制表符/管道/冒号），支持带引号的字段和转义字符。

**亮点总结**：
1. 自动检测 5 种常见分隔符
2. 支持带引号的字段和转义字符
3. 自动推断数据类型（整数/浮点/布尔/字符串）
4. 支持美化输出和流式生成
5. 支持跳过行和限制最大转换行数

**代替产品**：csvkit、jq、Pandas

---

### 14. `rotate_log` — 按大小轮转日志文件

**功能简介**：当日志文件达到指定大小时自动轮转备份，支持时间戳和序号两种命名方式，自动清理旧备份。

**亮点总结**：
1. 按文件大小触发轮转，防止日志无限增长
2. 支持时间戳和序号两种命名方式
3. 自动清理旧备份，保留最近 N 个
4. 轮转后自动创建新日志文件
5. 支持自定义备份目录和前缀

**代替产品**：logrotate、rotatelogs、newsyslog

---

### 15. `find_duplicates` — 查找重复内容文件

**功能简介**：通过哈希查找目录中的重复文件，支持 MD5/SHA1/SHA256 三种哈希算法，支持删除重复文件。

**亮点总结**：
1. 按大小预筛选 + 哈希精确匹配，高效查找
2. 支持 MD5/SHA1/SHA256 三种哈希算法
3. 支持删除重复文件（保留第一个）
4. 试运行预览模式，安全删除
5. 统计浪费空间，便于清理决策

**代替产品**：fdupes、rdfind、dupeGuru

---

### 16. `port_scan` — 扫描端口开放状态

**功能简介**：多线程 TCP 端口扫描器，支持自定义端口范围、超时时间、线程数，内置常见端口服务名映射。

**亮点总结**：
1. 多线程并发扫描（最多 256 线程），速度快
2. 非阻塞 connect + select 超时控制
3. 内置 40+ 常见端口服务名映射
4. 响应时间测量和显示
5. 支持 CSV 格式输出

**代替产品**：nmap、masscan、Unicornscan

---

### 17. `system_monitor` — 实时监控系统资源

**功能简介**：监控 CPU、内存、磁盘、网络、进程等系统资源使用情况，支持 CSV 日志输出和阈值告警。

**亮点总结**：
1. CPU 使用率计算（两次采样差值法）
2. 内存使用率（含缓存和缓冲区）
3. 磁盘使用率（统计所有挂载点）
4. 进程和线程数量统计
5. 支持 CSV 日志输出和阈值告警

**代替产品**：top、htop、Glances

---

### 18. `get_week_dates` — 获取本周日期列表

**功能简介**：获取本周或指定周的日期列表，支持自定义一周起始日（周一/周日），计算 ISO 周数。

**亮点总结**：
1. 支持自定义一周起始日（周一/周日）
2. 支持周偏移（上周/本周/下周）
3. 计算 ISO 周数
4. 支持多种日期格式
5. 显示星期几和中文星期名称

**代替产品**：date (Linux)、cal、ncal

---

### 19. `generate_histogram` — 生成数据直方图

**功能简介**：读取整数数据并生成文本直方图，使用 Sturges 公式自动计算最优分组数，支持水平/垂直两种方向。

**亮点总结**：
1. 使用 Sturges 公式自动计算最优分组数
2. 支持水平/垂直两种方向
3. 可显示频数和百分比
4. 自动计算平均值和标准差
5. 支持从文件读取数据

**代替产品**：gnuplot、histogram (Python)、R

---

### 20. `merge_sorted` — 合并两个已排序数组

**功能简介**：合并两个已排序数组，支持 int/long/double/string 四种数据类型，支持去重模式。

**亮点总结**：
1. 支持 int/long/double/string 四种类型
2. 内置泛型比较函数，可扩展自定义类型
3. 支持去重模式，自动移除重复元素
4. 时间复杂度 O(n+m)，空间复杂度 O(n+m)
5. 支持显示合并结果

**代替产品**：merge (Linux)、std::merge (C++)、pandas.concat

---

### 21. `tree` — 递归显示目录树结构

**功能简介**：以树形结构递归显示目录内容，支持颜色输出、显示文件大小和权限、排除模式和扩展名过滤。

**亮点总结**：
1. 支持颜色输出（目录蓝色/可执行绿色）
2. 显示文件大小和权限（可选）
3. 支持排除模式和扩展名过滤
4. 统计目录数和文件数
5. 支持最大深度限制

**代替产品**：tree (Linux)、ls -R、WinDirStat

---

### 22. `random_sample` — 从列表随机抽取样本

**功能简介**：从列表中随机抽取 N 个样本，支持无放回抽样。

**亮点总结**：
1. 无放回抽样，保证样本不重复
2. 使用 `rand()` 配合当前时间种子
3. 支持任意类型列表
4. 极简实现，代码量少

**代替产品**：random.sample (Python)、shuf (Linux)

---

### 23. `md5sum` — 计算文件 MD5 校验值

**功能简介**：完整自研 MD5 哈希算法（RFC 1321），流式处理支持大文件。

**亮点总结**：
1. 完整实现 MD5 算法（含 K 表、四轮变换、位操作）
2. 流式处理，支持大文件
3. 输出标准 MD5 格式（32 位十六进制）
4. 完全不依赖任何第三方库

**代替产品**：md5sum (Linux)、certutil (Windows)、md5 (macOS)

---

### 24. `is_process_running` — 检查进程是否运行

**功能简介**：通过进程名或 PID 检查进程是否正在运行，支持精确匹配和部分匹配两种模式。

**亮点总结**：
1. Linux：扫描 `/proc` 目录
2. Windows：使用 `CreateToolhelp32Snapshot`
3. 支持精确匹配和部分匹配两种模式
4. 可获取进程 PID、名称、完整路径
5. 支持显示进程详细信息

**代替产品**：ps、pgrep、tasklist (Windows)

---

### 25. `get_local_ip` — 获取本机 IP 地址

**功能简介**：获取本机所有网络接口的 IPv4/IPv6 地址，支持过滤回环接口和 IPv6 链路本地地址。

**亮点总结**：
1. Linux：使用 `getifaddrs()` 获取所有接口
2. Windows：使用 `GetAdaptersInfo()` 遍历网卡
3. 支持过滤回环接口和 IPv6 链路本地地址
4. 自动推荐默认 IP（第一个非回环 IPv4）
5. 支持显示接口名称和掩码

**代替产品**：ip addr、ifconfig、ipconfig (Windows)

---

### 26. `copy_to_clipboard` — 复制文本到剪贴板

**功能简介**：将文本复制到系统剪贴板，支持跨平台。

**亮点总结**：
1. Windows：使用 Win32 Clipboard API
2. Linux：自动检测并使用 `xclip` / `xsel` / `wl-copy`
3. 支持 PRIMARY 选择区（Linux X11）
4. 内存自动管理，剪贴板数据由系统托管
5. 支持长文本（64KB）

**代替产品**：xclip、pbcopy (macOS)、clip (Windows)

---

### 27. `timestamp` — 获取当前时间戳

**功能简介**：获取当前 Unix 时间戳，支持秒/毫秒/微秒/纳秒四种精度，支持多种格式。

**亮点总结**：
1. 支持秒/毫秒/微秒/纳秒四种精度
2. 支持 ISO8601/RFC3339/RFC2822/可读格式
3. 自动检测时区偏移并显示
4. 高精度时间获取
5. 显示星期和年第几天

**代替产品**：date +%s (Linux)、Get-Date (PowerShell)

---

### 28. `clear_cache` — 清理指定缓存文件

**功能简介**：递归清理缓存目录，支持按文件修改时间、文件大小、扩展名白名单过滤，支持排除特定目录。

**亮点总结**：
1. 按文件修改时间清理（保留最近 N 天）
2. 按文件大小过滤（最小/最大字节数）
3. 支持扩展名白名单（如 .tmp, .log, .cache）
4. 支持排除特定目录（如 .git, node_modules）
5. dry-run 预览模式，安全清理

**代替产品**：tmpwatch、tmpreaper、bleachbit

---

### 29. `file_type` — 获取文件 MIME 类型

**功能简介**：通过文件头魔数检测文件 MIME 类型，支持 200+ 种格式。

**亮点总结**：
1. 内置 300+ 魔数签名（JPEG/PNG/PDF/ZIP/ELF 等）
2. 支持文本文件检测（ASCII/UTF-8/JSON/XML/HTML）
3. 通过魔数长度计算置信度
4. 支持十六进制转储显示
5. 支持扩展名兜底检测

**代替产品**：file (Linux)、magic (Python)、TrID

---

### 30. `char_freq` — 统计字符出现频率

**功能简介**：统计文件或字符串中所有字符的出现频率，支持表格/直方图/CSV/JSON/列表五种输出格式。

**亮点总结**：
1. 统计所有字符（含不可打印字符）
2. 支持区分/不区分大小写
3. 支持五种输出格式（表格/直方图/CSV/JSON/列表）
4. 三种排序方式（字符/频率升序/频率降序）
5. 计算累计百分比和分类统计

**代替产品**：字符频率统计工具、Python collections.Counter

---

### 31. `fib` — 生成斐波那契数列

**功能简介**：支持 5 种算法生成斐波那契数列（迭代法、递归法（带缓存）、矩阵快速幂、比内公式、快速倍增法）。

**亮点总结**：
1. 五种算法覆盖不同应用场景
2. 自动检测 64 位溢出
3. 计算黄金比例近似值
4. 五种输出格式（列表/表格/CSV/JSON/序列）
5. 内置斐波那契数检测

**代替产品**：fibonacci (Python)、数学工具库

---

### 32. `verify_signature` — 验证文件数字签名

**功能简介**：验证文件的数字签名，支持 RSA/HMAC/PGP/文件哈希四种签名类型。

**亮点总结**：
1. 完整自研 SHA-256 哈希
2. 支持四种签名类型（RSA/HMAC/PGP/文件哈希）
3. 签名文件格式设计
4. 签名生成和验证
5. 校验和对比

**代替产品**：gpg、openssl dgst、cosign

---

### 33. `cd` — 切换当前工作目录

**功能简介**：切换当前工作目录，支持 `cd ~`、`cd -`、`cd ..` 等多种快捷语法，记录目录访问历史。

**亮点总结**：
1. 支持 `cd ~`、`cd -`、`cd ..` 等多种快捷语法
2. 记录目录访问历史，支持前进/后退
3. 跨平台路径规范化（Windows/Linux）
4. 权限检查：可读性、可执行性验证
5. 支持切换后显示当前目录

**代替产品**：cd (Shell builtin)、pushd/popd

---

### 34. `qr_generate` — 生成二维码图片

**功能简介**：将文本编码为 QR 码，保存为 BMP 图像，支持 Reed-Solomon 纠错码，完全自研不依赖任何二维码库。

**亮点总结**：
1. 完整自研 QR 码编码器（Reed-Solomon 纠错）
2. 7 种 QR 版本支持（1-7）
3. 四种纠错级别（L/M/Q/H）
4. 自研 BMP 图像编码器
5. 掩码模式选择
6. 完全不依赖任何二维码库

**代替产品**：qrencode、zxing、pyqrcode

---

### 35. `dedup` — 去除文件中重复行

**功能简介**：去除文件中重复行，支持保留首次/保留最后/不区分大小写/去空格后去重四种模式。

**亮点总结**：
1. 四种去重模式覆盖不同需求
2. 使用哈希表实现 O(n) 时间复杂度
3. 支持排序输出和显示重复次数
4. 支持忽略空行和注释行
5. 处理超大文件（百万行级别）

**代替产品**：sort -u、uniq (Linux)、awk

---

### 36. `disk_usage` — 计算磁盘使用率

**功能简介**：获取磁盘分区使用情况（总容量/已用/可用/使用率），支持所有挂载点自动检测。

**亮点总结**：
1. Linux：解析 `/proc/mounts` 和 `statvfs`
2. Windows：使用 `GetDiskFreeSpaceEx`
3. 支持所有挂载点自动检测
4. 显示进度条和颜色告警（黄/红）
5. 区分可移动/网络/只读设备

**代替产品**：df (Linux)、Get-PSDrive (PowerShell)

---

### 37. `uuid_generate` — 生成 UUID 唯一标识

**功能简介**：生成符合 RFC 4122 标准的 UUID v1（时间戳）和 v4（随机），支持批量生成。

**亮点总结**：
1. 支持 UUID v1（时间戳）和 v4（随机）
2. 使用 `/dev/urandom`（Linux）或 `CryptGenRandom`（Windows）
3. 自动获取 MAC 地址作为节点 ID（v1）
4. 支持批量生成、大写/小写、带/无连字符
5. 支持 JSON 格式输出

**代替产品**：uuidgen (Linux)、New-Guid (PowerShell)

---

### 38. `whoami` — 获取当前用户名

**功能简介**：获取当前登录用户的用户名、域名、用户组、主目录、Shell 等信息。

**亮点总结**：
1. Linux：解析 `/etc/passwd` 和 `/proc/self/status`
2. Windows：使用 `GetUserNameExA` 和 `LookupAccountSid`
3. 获取 UID/GID、主目录、Shell、用户组列表
4. 自动检测管理员/root 特权
5. 支持显示完整用户信息

**代替产品**：whoami (Linux/Windows)、id (Linux)

---

### 39. `classify_by_extension` — 按扩展名分类文件

**功能简介**：扫描目录，按扩展名分类文件并移动到对应文件夹，支持按类型分组（图片/文档/压缩/音频/视频/代码/可执行等）。

**亮点总结**：
1. 按扩展名自动创建分类文件夹
2. 支持按类型分组（内置 80+ 种文件类型映射）
3. 支持移动/复制/符号链接三种操作模式
4. 支持保留目录结构
5. 支持排除模式和扩展名过滤
6. 试运行预览模式

**代替产品**：FileOrganizer、DropIt、Belvedere

---

### 40. `auto_save` — 定时自动保存文件

**功能简介**：监控文件变化，定时自动保存备份，支持三种保存模式（定时/变化时/两者结合）和三种备份策略（版本化/时间戳/增量）。

**亮点总结**：
1. 三种保存模式灵活配置
2. 三种备份策略覆盖不同需求
3. 支持守护进程模式（后台运行）
4. 自动清理旧版本
5. 支持文件变化实时检测

**代替产品**：autosave (IDE插件)、备份软件

---

### 41. `encrypt_backup` — 加密备份文件

**功能简介**：使用 XOR+滚动密钥加密备份文件，支持密码保护和完整性校验，密码派生使用 PBKDF2 风格。

**亮点总结**：
1. 自研 XOR 加密 + 加盐密钥派生
2. 密码派生使用 PBKDF2 风格（多次哈希迭代）
3. 加密头包含魔数、版本、盐、校验和
4. 支持完整性校验（解密时验证）
5. 可配置密钥轮数（迭代次数）

**代替产品**：gpg、openssl enc、7-Zip（加密）

---

### 42. `batch_create_users` — 批量创建系统用户

**功能简介**：批量创建系统用户账户，支持随机密码生成、用户组分配、从文件读取用户列表。

**亮点总结**：
1. Linux：使用 `useradd` 和 `chpasswd`
2. Windows：使用 `NetUserAdd` API
3. 支持随机密码生成（含特殊字符）
4. 支持用户组分配
5. 支持从文件读取用户列表（批量导入）
6. 试运行预览模式

**代替产品**：useradd (Linux)、New-LocalUser (PowerShell)

---

### 43. `search_replace` — 全局搜索替换文本

**功能简介**：在指定文件或目录中全局搜索并替换文本内容，支持四种搜索模式（精确/不区分大小写/全词匹配/正则）和三种替换模式（替换首次/替换全部/整行替换）。

**亮点总结**：
1. 四种搜索模式覆盖不同场景
2. 三种替换模式灵活应用
3. 支持文件备份（自动生成 .bak）
4. 支持递归目录扫描
5. 显示替换统计和匹配数

**代替产品**：sed、awk、replace (Python)

---

### 44. `get_depth` — 计算目录嵌套深度

**功能简介**：计算指定目录的最大嵌套深度，支持树形结构显示。

**亮点总结**：
1. 递归计算最大深度
2. 支持树形结构显示
3. 支持排除目录和深度限制
4. 深度分级告警（>10层警告，>20层严重）
5. 统计目录数和文件数

**代替产品**：find、tree -d

---

### 45. `get_arch` — 检测系统架构类型

**功能简介**：检测当前系统的 CPU 架构类型，支持 16 种架构（x86/x64/ARM/ARM64/IA64/PPC/MIPS/RISC-V/S390/SPARC/LoongArch）。

**亮点总结**：
1. 支持 16 种架构检测
2. 支持字节序检测（小端/大端）
3. 获取 CPU 厂商、型号、核心数
4. 支持 JSON 和 CSV 格式输出
5. 支持显示操作系统信息

**代替产品**：arch、uname -m、wmic os get osarchitecture

---

### 46. `batch_chmod` — 批量修改文件权限

**功能简介**：批量修改文件或目录的权限，支持数字模式（755/644）和符号模式（u+x/g-w），支持参考文件模式。

**亮点总结**：
1. 支持数字模式和符号模式
2. 支持参考文件模式（从另一个文件复制权限）
3. 按文件/目录/可执行文件三种目标过滤
4. 支持试运行预览
5. 支持递归处理子目录

**代替产品**：chmod (Linux)、icacls (Windows)

---

### 47. `date_range` — 生成日期范围列表

**功能简介**：生成指定范围内的日期列表，支持日/周/月/年/工作日/周末六种步长，支持列表/CSV/JSON/表格/范围五种输出格式。

**亮点总结**：
1. 六种步长覆盖不同场景
2. 自动计算闰年和每月天数
3. 支持 ISO 周数和年第几天
4. 五种输出格式灵活选择
5. 星期分布统计

**代替产品**：dateutils、Python datetime、R lubridate

---

### 48. `is_file_locked` — 检测文件是否被锁定

**功能简介**：检测文件是否被其他进程锁定或占用，获取锁定进程 PID、进程名、进程路径。

**亮点总结**：
1. Linux：使用 `fcntl F_GETLK` 检测锁
2. Windows：使用 `CreateFile` 共享冲突检测
3. 获取锁定进程 PID、进程名、进程路径
4. 区分独占锁和共享锁
5. 显示文件大小和修改时间

**代替产品**：lsof (Linux)、handle (Windows)、LockHunter

---

### 49. `batch_chown` — 批量修改文件所有者

**功能简介**：批量修改文件或目录的所有者和用户组，支持单独或同时修改。

**亮点总结**：
1. 支持修改所有者和用户组（单独或同时）
2. Linux：使用 `chown`/`lchown` 系统调用
3. Windows：使用 `icacls` 命令（需管理员权限）
4. 支持递归处理子目录
5. 支持符号链接处理

**代替产品**：chown (Linux)、Takeown (Windows)

---

### 50. `ping_test` — 测试网络延迟

**功能简介**：测试网络延迟和丢包率，支持 ICMP ping，计算最小/最大/平均 RTT 和标准差。

**亮点总结**：
1. Linux：使用原始套接字 + 手写 ICMP 校验和
2. Windows：使用 `IcmpSendEcho` API
3. 支持连续 ping 和自定义数据包大小
4. 计算最小/最大/平均 RTT 和标准差
5. 网络质量评估（优秀/良好/可用/较差/很差）

**代替产品**：ping (Linux/Windows)、fping

---

### 51. `disk_io_monitor` — 监控磁盘读写速度

**功能简介**：实时监控磁盘读写速度和 IOPS，计算读/写速度（MB/s）、IOPS、延迟、队列长度和利用率。

**亮点总结**：
1. Linux：解析 `/sys/block/*/stat`
2. 计算读/写速度（MB/s）、IOPS、延迟
3. 监控队列长度和利用率
4. 支持多磁盘同时监控
5. 彩色告警（黄/红）

**代替产品**：iostat、iotop、dstat

---

### 52. `compress_text` — 压缩文本数据

**功能简介**：使用游程编码（RLE）压缩文本数据，支持压缩/解压双向。

**亮点总结**：
1. 游程编码压缩连续重复字符
2. 支持任意文本内容
3. 压缩/解压双向支持
4. 压缩率可视化显示
5. 极简实现

**代替产品**：gzip、bzip2、zlib

---

### 53. `process_tree` — 显示进程树结构

**功能简介**：以树形结构显示系统进程层次关系，构建父子进程关系树。

**亮点总结**：
1. 构建父子进程关系树
2. 计算进程深度
3. 支持显示 PID/用户/状态/线程数
4. 支持按 PID/用户/内存/CPU 过滤
5. 统计僵尸进程数量

**代替产品**：pstree、ps --forest、Process Explorer

---

### 54. `split_file` — 按大小分割文件

**功能简介**：按指定大小将文件分割成多个部分，支持按大小/行数/份数/块数四种分割模式。

**亮点总结**：
1. 四种分割模式覆盖不同场景
2. 支持 K/M/G/T 单位解析
3. 支持自定义编号位数
4. 显示进度和分割统计
5. 支持大文件（64 位寻址）

**代替产品**：split (Linux)、gsplit (macOS)、HJSplit

---

### 55. `connection_pool` — 创建连接池

**功能简介**：创建和管理数据库/网络连接池，支持 TCP/UDP/Unix 域套接字，自动连接复用和超时检测。

**亮点总结**：
1. 支持 TCP/UDP/Unix 域套接字
2. 自动连接复用和超时检测
3. 支持最大连接数和最小连接数
4. 自动重连和健康检查
5. 使用计数限制和空闲超时

**代替产品**：HikariCP、c3p0、Apache Commons Pool

---

### 56. `cache_manager` — 管理缓存大小

**功能简介**：LRU 缓存管理，支持自动淘汰和统计，线程安全。

**亮点总结**：
1. 完整的 LRU 缓存实现（双向链表 + 哈希表）
2. 线程安全（pthread 读写锁）
3. 命中率统计和缓存大小控制
4. 支持自动淘汰（LRU/LFU/混合）
5. 支持缓存项过期时间（TTL）

**代替产品**：Redis、Memcached、Guava Cache

---

### 57. `batch_symlink` — 批量创建符号链接

**功能简介**：批量创建符号链接，支持递归和覆盖，支持绝对路径和相对路径两种模式。

**亮点总结**：
1. Linux：使用 `symlink()` 系统调用
2. Windows：使用 `CreateSymbolicLinkA` API
3. 支持保留目录结构或扁平结构
4. 支持绝对路径和相对路径两种模式
5. 支持试运行预览模式

**代替产品**：ln (Linux)、mklink (Windows)

---

### 58. `realtime_log` — 实时监控日志文件

**功能简介**：实时监控日志文件变化，类似 `tail -f`，支持日志轮转检测（inode 变化）。

**亮点总结**：
1. 支持 `tail -f` 风格实时监控
2. 日志轮转检测（inode 变化）
3. 彩色高亮（ERROR红色/WARN黄色/INFO青色）
4. 支持行号显示和时间戳
5. 支持文本过滤（区分/不区分大小写）

**代替产品**：tail -f、less +F、multitail

---

### 59. `remove_empty_files` — 删除所有空文件

**功能简介**：递归删除目录中所有空文件（大小为0字节），可选删除空目录。

**亮点总结**：
1. 递归处理所有子目录
2. 可选删除空目录
3. 支持试运行和确认模式
4. 支持排除模式和扩展名过滤
5. 统计释放空间和删除文件数

**代替产品**：find -empty -delete、rm -f

---

### 60. `filename_hash` — 计算文件名哈希值

**功能简介**：计算文件名的哈希值，支持 8 种哈希算法（CRC32/DJB2/FNV-1a/Jenkins/Murmur3/MD5/SHA1/SHA256）。

**亮点总结**：
1. 支持 8 种哈希算法
2. 支持四种输出格式（十六进制/十进制/二进制/Base64）
3. 支持递归目录扫描
4. 支持排除模式和扩展名过滤
5. 支持 CSV/JSON 输出

**代替产品**：filename hash tools、md5sum

---

### 61. `detect_mime` — 检测文件 MIME 类型

**功能简介**：通过文件头魔数检测 MIME 类型，内置 90+ 种文件魔数签名，支持图像/视频/音频/文档/压缩/可执行/文本/数据库/字体/抓包等类型。

**亮点总结**：
1. 内置 90+ 种魔数签名
2. 支持图像/视频/音频/文档/压缩/可执行/文本/数据库/字体/抓包等类型
3. 支持十六进制显示
4. 支持扩展名兜底检测
5. 置信度评分系统

**代替产品**：file (Linux)、magic (Python)

---

### 62. `temperature_monitor` — 监控 CPU 温度

**功能简介**：实时监控 CPU 温度，支持多传感器（多核心），温度阈值告警（警告/严重/紧急）。

**亮点总结**：
1. Linux：扫描 `/sys/class/thermal` 和 `/sys/class/hwmon`
2. Windows：使用 WMI 查询
3. 支持多传感器（多核心）
4. 温度阈值告警（警告/严重/紧急）
5. 温度历史记录

**代替产品**：sensors (Linux)、Core Temp、HWMonitor

---

### 63. `batch_metadata` — 批量提取文件元数据

**功能简介**：批量提取文件的元数据（大小/时间/权限/所有者等），支持表格/CSV/JSON/XML/人类可读五种输出格式。

**亮点总结**：
1. 提取 20+ 种元数据字段
2. 五种输出格式灵活选择
3. 支持递归目录扫描
4. 支持排除模式和扩展名过滤
5. 统计总大小和平均文件大小

**代替产品**：stat、exiftool、MetaExtractor

---

### 64. `integrity_check` — 校验文件完整性

**功能简介**：通过哈希校验文件完整性，完整自研 SHA-256/SHA-1/MD5 三种哈希，支持创建/验证/更新/列出四种模式。

**亮点总结**：
1. 完整自研三种哈希算法
2. 四种操作模式（创建/验证/更新/列出）
3. 生成标准清单文件
4. 支持递归目录扫描
5. 统计通过/修改/缺失文件数

**代替产品**：sha256sum、md5sum、Tripwire

---

### 65. `set_cpu_affinity` — 设置进程 CPU 亲和性

**功能简介**：设置进程绑定到指定 CPU 核心，支持单个核心、核心范围、核心列表、偶数/奇数核心等选择模式。

**亮点总结**：
1. Linux：使用 `sched_setaffinity`
2. Windows：使用 `SetProcessAffinityMask`
3. 多种核心选择模式
4. 支持获取当前亲和性并显示
5. 支持递归设置子进程

**代替产品**：taskset (Linux)、SetProcessAffinityMask (Windows)

---

### 66. `simple_encrypt` — 简单字符串加密

**功能简介**：使用 XOR + 滚动密钥加密/解密字符串，支持 Base64 编码输出，内置校验和验证。

**亮点总结**：
1. 自研 XOR + 滚动密钥（密钥随位置变化）
2. 支持 Base64 编码输出
3. 内置校验和验证（防篡改）
4. 加密/解密双向验证
5. 支持文件加密（可选）

**代替产品**：openssl enc、gpg -c

---

### 67. `sync_folders` — 同步两个目录

**功能简介**：同步两个目录保持内容一致，支持四种同步方向（源→目标/目标→源/双向/镜像）和五种冲突策略。

**亮点总结**：
1. 四种同步方向灵活配置
2. 五种冲突策略（跳过/覆盖/重命名/保留较新/保留较旧）
3. 支持删除多余文件
4. 保留时间戳和权限
5. 支持 dry-run 预览

**代替产品**：rsync、robocopy、Syncthing

---

### 68. `hot_reload` — 热加载配置文件

**功能简介**：监控配置文件变化并自动热加载，支持 Linux inotify 和 Windows ReadDirectoryChangesW，自动备份旧配置。

**亮点总结**：
1. Linux：使用 `inotify` 实时监控
2. Windows：使用 `ReadDirectoryChangesW`
3. 自动备份旧配置（带时间戳）
4. 解析失败自动回滚
5. 支持 SIGHUP 信号触发重载

**代替产品**：supervisor - reread、systemctl reload、nginx -s reload

---

### 69. `batch_decrypt` — 批量解密文件

**功能简介**：批量解密加密文件，支持 XOR/XOR加盐/ROT13 三种算法，密钥派生使用 PBKDF2 风格。

**亮点总结**：
1. 支持三种算法（XOR/XOR加盐/ROT13）
2. 密钥派生使用 PBKDF2 风格迭代哈希
3. 支持递归目录扫描
4. 支持保留目录结构
5. 解密后可选删除原加密文件

**代替产品**：openssl enc -d、gpg -d

---

### 70. `hardlink_dir` — 创建目录硬链接

**功能简介**：递归为目录下所有文件创建硬链接副本，保留时间戳和权限，跨平台支持。

**亮点总结**：
1. Linux：使用 `link()` 系统调用
2. Windows：使用 `CreateHardLinkA`
3. 三种冲突策略（跳过/覆盖/重命名）
4. 保留时间戳和权限
5. 硬链接跨平台一致性

**代替产品**：rsync --link-dest、cp -lr、ln

---

### 71. `packet_capture` — 捕获网络数据包

**功能简介**：纯自研网络包捕获工具（迷你 tcpdump），完整解析以太网/IP/TCP/UDP/ICMP 头，BPF 风格过滤器。

**亮点总结**：
1. Linux：使用 AF_PACKET 原始套接字
2. 完整解析以太网/IP/TCP/UDP/ICMP 头
3. BPF 风格过滤器（IP/端口/协议）
4. 五元组提取和载荷显示
5. Hex dump 和 ASCII 输出

**代替产品**：tcpdump、Wireshark、tshark

---

### 72. `batch_convert_encoding` — 批量转换文件编码

**功能简介**：批量检测并转换文件字符编码，支持 17 种编码自动检测，自研 UTF-8↔GBK 转换映射表。

**亮点总结**：
1. 支持 17 种编码自动检测
2. 自研 UTF-8↔GBK 转换映射表
3. 完整 BOM 支持（添加/移除）
4. 支持递归目录扫描和保留结构
5. 支持 dry-run 和自动备份

**代替产品**：iconv、enca、recode

---

### 73. `process_watchdog` — 进程守护自动重启

**功能简介**：监控进程，崩溃后自动重启，支持三种监控模式（PID/进程名/执行命令）和四种重启策略。

**亮点总结**：
1. 三种监控模式（PID/进程名/执行命令）
2. 四种重启策略（总是/崩溃时/退出时/限制次数）
3. 指数退避重启（防止频繁重启）
4. PID 文件管理
5. 支持以指定用户运行

**代替产品**：supervisor、monit、systemd

---

### 74. `split_large_file` — 分割大文件

**功能简介**：将大文件按大小/行数/份数分割，支持 64 位大文件（>2GB），四种命名风格。

**亮点总结**：
1. 支持 64 位大文件（>2GB）
2. 三种分割模式（大小/行数/份数）
3. 四种命名风格（suffix/extension/prefix/number）
4. 内置 CRC32 校验
5. 流式处理，内存占用小

**代替产品**：split、dd、HJSplit

---

### 75. `cache_service` — 内存缓存服务

**功能简介**：高性能内存缓存服务，支持 LRU/LFU/FIFO/TTL/混合五种淘汰策略，RDB 快照持久化 + AOF 日志。

**亮点总结**：
1. 五种淘汰策略（LRU/LFU/FIFO/TTL/混合）
2. 一致性哈希集群支持
3. RDB 快照持久化 + AOF 日志
4. 自研网络协议（SET/GET/DEL/EXISTS/INCR/DECR/EXPIRE/TTL/FLUSH/STATS/BACKUP）
5. 主从复制支持

**代替产品**：Redis、Memcached、etcd

---

### 76. `tail` — 监控文件尾行

**功能简介**：读取文件末尾 N 行，支持持续监控（tail -f），日志轮转检测（inode 变化）。

**亮点总结**：
1. 从文件末尾向前搜索，高效读取最后 N 行
2. 支持 `tail -f` 风格实时监控
3. 日志轮转检测（inode 变化）
4. 五种输出格式（原始/时间戳/行号/JSON/CSV）
5. 彩色高亮（ERROR红色/WARN黄色/INFO青色）

**代替产品**：tail (Linux)、PowerShell Get-Content -Wait

---

### 77. `dir_stat` — 统计目录占用

**功能简介**：深度目录分析，包含大小/文件数/最大文件排行/扩展名分类统计，五种输出模式。

**亮点总结**：
1. 按扩展名分类统计（自动排序显示 Top 20）
2. 最大文件 Top N 和最大目录 Top N
3. 五种输出模式（摘要/详细/扩展名/CSV/JSON）
4. 64 位大文件支持
5. 进度显示和深度限制

**代替产品**：du、ncdu、TreeSize

---

### 78. `auto_complete` — 自动补全文件名

**功能简介**：智能文件名补全，支持前缀匹配、模糊匹配、通配符、正则、智能匹配、最近使用六种补全模式。

**亮点总结**：
1. 六种补全模式覆盖不同场景
2. Levenshtein 距离模糊匹配
3. 历史学习机制（记录使用频率）
4. 综合评分排序（多维度权重）
5. 目录优先和扩展名优先

**代替产品**：bash-completion、fzf、zoxide

---

### 79. `convert_line_endings` — 转换行尾格式

**功能简介**：批量转换文本文件行尾格式（CRLF/LF/CR），自动检测当前行尾格式，支持 CRLF ↔ LF ↔ CR 任意互转。

**亮点总结**：
1. 自动检测当前行尾格式（CRLF/LF/CR/混合）
2. 支持 CRLF ↔ LF ↔ CR 任意互转
3. 二进制文件自动跳过
4. 递归目录处理
5. 格式分布统计

**代替产品**：dos2unix、unix2dos、flip

---

### 80. `dir_diff` — 对比两个目录差异

**功能简介**：递归对比两个目录，识别新增、删除、修改的文件，支持大小/修改时间/哈希值三重对比。

**亮点总结**：
1. 三重对比机制（大小/修改时间/哈希值）
2. 六种差异类型识别（新增/缺失/修改/大小变化/时间变化/类型变化/权限变化）
3. 四种输出格式（人类可读/CSV/JSON/补丁脚本）
4. 自研轻量级文件哈希算法
5. 补丁脚本生成，一键同步差异

**代替产品**：diff、rsync -n、meld

---

### 81. `batch_rename_ext` — 批量修改扩展名

**功能简介**：批量修改文件的扩展名，支持替换、添加、删除、大小写转换、前缀/后缀添加六种操作模式。

**亮点总结**：
1. 六种扩展名操作模式
2. 智能扩展名解析（正确处理无扩展名和多点文件名）
3. 递归批量处理
4. 支持 dry-run 预览模式
5. 完整统计信息

**代替产品**：rename、PowerShell Rename-Item、Bulk Rename Utility

---

### 82. `check_permissions` — 查看文件权限掩码

**功能简介**：查看文件的权限掩码，支持数值模式、符号模式、人类可读三种输出格式，显示所有者、用户组和特殊权限位。

**亮点总结**：
1. 三种输出格式（数值/符号/人类可读）
2. 显示所有者、用户组和特殊权限位（SUID/SGID/Sticky）
3. 支持递归目录扫描
4. 支持 JSON/CSV 输出
5. 跨平台支持

**代替产品**：stat、ls -l、icacls (Windows)

---

### 83. `clean_directory` — 按时间清理目录

**功能简介**：按文件时间（访问/修改/创建）清理目录中的旧文件，支持删除、移动、截断三种操作。

**亮点总结**：
1. 三种时间标准（访问/修改/创建）
2. 三种清理模式（早于/晚于/之间）
3. 三种操作类型（删除/移动/截断）
4. 支持试运行预览
5. 支持排除模式和扩展名过滤

**代替产品**：tmpwatch、tmpreaper、bleachbit

---

### 84. `version_backup` — 版本备份文件

**功能简介**：为文件创建带版本号的备份，支持数字版本、时间戳、日期时间、序列号、Git风格五种命名模式。

**亮点总结**：
1. 五种版本命名模式
2. 自动清理旧版本（保留最近 N 个）
3. 版本历史查看
4. 保留时间戳
5. 支持试运行预览

**代替产品**：git、RCS、etckeeper

---

### 85. `process_memory` — 查看进程内存占用

**功能简介**：查看指定进程或所有进程的内存占用详情，显示 RSS、VSZ、共享内存、私有内存、内存映射等详细信息。

**亮点总结**：
1. 跨平台内存信息获取
2. 显示 RSS、VSZ、共享、私有、交换、峰值等详细指标
3. 支持按进程名/PID 查询
4. 支持排行榜模式（Top N）
5. 支持 JSON/CSV 输出

**代替产品**：top、htop、ps、Process Explorer

---

### 86. `quick_launch` — 快速启动应用

**功能简介**：快速启动应用程序，支持进程检测（如果已在运行则不重复启动）。

**亮点总结**：
1. 进程检测防重复启动
2. 支持命令行参数
3. 跨平台支持
4. 极简实现

**代替产品**：Start-Process (PowerShell)、open (macOS)、xdg-open (Linux)

---

### 87. `batch_set_ctime` — 批量修改创建时间

**功能简介**：批量修改文件的创建时间。

**亮点总结**：
1. 批量修改创建时间
2. 支持递归目录扫描
3. 跨平台时间设置
4. 支持排除模式和扩展名过滤

**代替产品**：touch、SetFile (macOS)、PowerShell

---

### 88. `password_strength` — 检测密码强度

**功能简介**：检测密码强度，综合长度、字符集多样性、特殊字符等因素评分。

**亮点总结**：
1. 多维度评分（长度/大小写/数字/特殊字符）
2. 0-5 分制评分
3. 强度等级划分（极弱/弱/中等/强/极强）
4. 极简实现

**代替产品**：zxcvbn、HaveIBeenPwned、密码强度检测器

---

### 89. `speed_test` — 测试网络下载速度

**功能简介**：测试网络下载速度。

**亮点总结**：
1. 下载速度测试
2. 计算平均速度
3. 支持自定义测试文件

**代替产品**：speedtest-cli、iperf、fast.com

---

### 90. `convert_audio` — 转换音频格式

**功能简介**：转换音频文件格式。

**亮点总结**：
1. 支持多种音频格式
2. 保留音质参数
3. 批量转换

**代替产品**：ffmpeg、sox、Audacity

---

### 91. `video_thumbnail` — 生成视频缩略图

**功能简介**：从视频中截取指定时间点生成缩略图。

**亮点总结**：
1. 指定时间点截取
2. 自定义缩略图尺寸
3. 支持批量处理

**代替产品**：ffmpeg、Video Thumbnailer

---

### 92. `batch_resize` — 批量调整图片大小

**功能简介**：批量调整图片尺寸。

**亮点总结**：
1. 自定义宽度和高度
2. 保持宽高比选项
3. 批量处理

**代替产品**：ImageMagick、ffmpeg、批量图片处理工具

---

### 93. `generate_album` — 生成图片相册

**功能简介**：扫描目录生成 HTML 图片相册。

**亮点总结**：
1. 自动扫描图片文件
2. 生成 HTML 相册页面
3. 支持缩略图显示

**代替产品**：Gallery Generator、ffmpeg、自行编写 HTML

---

### 94. `text_to_speech` — 文字转语音

**功能简介**：将文本转换为语音。

**亮点总结**：
1. 跨平台语音合成
2. 支持多种语音参数
3. 输出 WAV 文件

**代替产品**：espeak、Festival、TTS (Python)

---

### 95. `record_audio` — 录音保存文件

**功能简介**：录音并保存为音频文件。

**亮点总结**：
1. 指定录音时长
2. 保存为 WAV 格式
3. 跨平台支持

**代替产品**：arecord (Linux)、Sound Recorder (Windows)

---

### 96. `qr_decode` — 解码二维码内容

**功能简介**：从图像文件中解码二维码内容。

**亮点总结**：
1. 从图像文件解码 QR 码
2. 输出解码文本内容
3. 跨平台支持

**代替产品**：zbarimg、zxing、QR Code Reader

---
---

97. barcode_generate — 生成条形码

功能简介：将字符串编码为 Code 128 条形码，输出 BMP 图像。

依赖库：无（完全自研）

亮点解析：

1. 完全自研 Code 128 编码逻辑，包含起始符、数据编码、校验位计算和终止符
2. 内置 BMP 图像编码器，不依赖任何图像库
3. 支持自定义模块宽度、条形高度、边距和静区
4. 包含完整的错误处理、内存管理和参数校验
5. 跨平台兼容 Windows 和 Linux

实现难度：复杂

代替产品：ZBar、libdmtx、ZXing

---

98. style_transfer — 图片风格迁移

功能简介：将一幅图像的风格迁移到另一幅图像上，基于直方图匹配和纹理合成。

依赖库：无（完全自研，需 -lm）

亮点解析：

1. 完整自研 RGB ↔ LAB 色彩空间转换
2. 基于直方图匹配的全局色彩风格迁移
3. 多尺度纹理特征提取（局部方差）
4. 多尺度金字塔架构，从粗到细逐级迁移
5. 完全自研 BMP 图像 I/O，零外部依赖

实现难度：地狱

代替产品：DeepArt、Prisma、Neural Style Transfer

---

99. video_crop — 裁剪视频画面

功能简介：从 AVI 视频中裁剪指定区域，输出为新的 AVI 视频文件。

依赖库：无（完全自研）

亮点解析：

1. 完全自研 AVI 容器解析与生成（RIFF/AVI 格式）
2. 流式逐帧处理，支持大视频文件
3. 智能裁剪模式（指定坐标或自动居中）
4. 完整 AVI 索引支持，确保播放器正常寻址
5. 包含帧对齐、BMP 行填充处理、进度显示

实现难度：复杂

代替产品：FFmpeg、VirtualDub、Avidemux

---

100. merge_audio_video — 合并音视频

功能简介：将 WAV 音频文件与 AVI 视频文件合并为带声音的视频文件。

依赖库：无（完全自研）

亮点解析：

1. 完全自研 WAV 和 AVI 容器格式读写
2. 音视频帧级精确同步（三种同步模式）
3. 支持 8-bit/16-bit PCM 格式互转和音量增益
4. 流式处理架构，内存占用恒定
5. 输出包含完整双流（视频+音频）和索引

实现难度：地狱

代替产品：FFmpeg、MKVToolNix、Avidemux

---

101. video_add_subtitle — 添加视频字幕

功能简介：为 AVI 视频文件添加硬编码字幕（烧录到画面中）。

依赖库：无（完全自研）

亮点解析：

1. 完全自研 SRT 字幕解析器（支持 UTF-8/GBK）
2. 内置 5x7 点阵字体渲染引擎（95 个 ASCII 字符）
3. 支持字幕背景框、颜色、描边和大小缩放
4. 精确时间同步（毫秒级精度）
5. 三种布局模式（底部居中、顶部居中、自定义）

实现难度：复杂

代替产品：FFmpeg、Aegisub、HandBrake

---

102. audio_fade — 音频淡入淡出

功能简介：为 WAV 音频文件添加淡入和淡出效果。

依赖库：无（完全自研，需 -lm）

亮点解析：

1. 完全自研 WAV 格式解析与生成
2. 三种淡入淡出曲线（线性、对数、指数）
3. 独立控制淡入和淡出时长及起始/结束音量
4. 支持 8-bit 和 16-bit PCM 格式
5. 流式处理架构，支持超大音频文件

实现难度：中等

代替产品：Audacity、SoX、FFmpeg

---

103. audio_equalizer — 音频均衡器

功能简介：对 WAV 音频文件进行 10 段参量均衡处理。

依赖库：无（完全自研，需 -lm）

亮点解析：

1. 完整 IIR 双二阶滤波器（Biquad）实现
2. 10 段专业均衡频段（31Hz–16kHz），±12dB 增益
3. 支持单声道和立体声（左右独立均衡）
4. 支持 8-bit 和 16-bit PCM 格式
5. 流式处理架构，内存占用恒定

实现难度：复杂

代替产品：Audacity、SoX、Adobe Audition

---

104. mouse_tracker — 追踪鼠标轨迹

功能简介：实时追踪鼠标移动轨迹，记录坐标和时间戳，输出为日志文件。

依赖库：Windows: -lgdi32；Linux: -lX11 -lpthread

亮点解析：

1. 跨平台鼠标事件捕获（Windows 钩子 / X11 轮询）
2. 支持鼠标移动、按下、释放、滚轮事件
3. 可配置采样间隔（默认 50ms）
4. CSV 格式日志输出，含时间戳和事件类型
5. 自动生成事件统计报告

实现难度：简单

代替产品：Windows Spy++、xev (Linux)、AutoHotkey

---

105. auto_clicker — 自动点击器

功能简介：在指定位置或当前鼠标位置自动执行点击操作。

依赖库：Windows: -lgdi32；Linux: -lX11 -lXtst -lpthread

亮点解析：

1. 跨平台输入模拟（Windows mouse_event / X11 XTest）
2. 支持左/右/中键单击、双击、长按（9 种类型）
3. 四种轨迹模式（固定、跟随鼠标、圆形、矩形、螺旋）
4. 可调节点击间隔、总次数、长按时间
5. 实时进度反馈

实现难度：中等

代替产品：AutoHotkey、xdotool、GS Auto Clicker

---

106. auto_arrange_windows — 自动排列窗口

功能简介：自动排列系统中所有打开的窗口。

依赖库：Windows: -lgdi32 -ldwmapi；Linux: -lX11

亮点解析：

1. 跨平台窗口枚举和操作（Windows EnumWindows / X11 XQueryTree）
2. 七种排列模式（平铺、层叠、网格、纵向、横向、圆形、螺旋）
3. 按标题/类名/PID 包含/排除窗口
4. 可调节边距、间距、最小窗口尺寸
5. 自动检测屏幕尺寸和任务栏高度

实现难度：中等

代替产品：FancyZones、GridMove、i3wm

---

107. gesture_recognition — 识别鼠标手势

功能简介：实时识别鼠标绘制的轨迹手势（8 方向 + DTW 算法）。

依赖库：Windows: -lgdi32；Linux: -lX11 -lXtst -lpthread

亮点解析：

1. 8 方向手势编码（上/下/左/右/四角）
2. DTW 动态时间规整算法匹配不同长度手势
3. 支持自定义手势模板添加
4. 预置 12 个默认模板（箭头和字母 V/N/Z/L）
5. 实时轨迹采样与置信度输出

实现难度：复杂

代替产品：StrokeIt、EasyStroke、Windows Ink

---

108. clipboard_history — 剪贴板历史

功能简介：监控剪贴板内容变化，记录文本和图片历史。

依赖库：Windows: -lgdi32 -luser32；Linux: -lX11 -lpthread

亮点解析：

1. 跨平台剪贴板监控（Windows 查看器链 / X11 轮询）
2. 支持文本和图片两种内容类型
3. 智能去重（内容哈希）
4. 支持列表查看、搜索、清除、导出
5. 可配置最大历史记录数（默认 1000）

实现难度：中等

代替产品：Ditto、CopyQ、ClipClip

---

109. hotkey_bind — 绑定快捷键

功能简介：注册系统级全局快捷键，绑定到自定义命令。

依赖库：Windows: -lgdi32 -luser32；Linux: -lX11 -lpthread

亮点解析：

1. 跨平台全局热键注册（Windows RegisterHotKey / X11 键位抓取）
2. 支持 Ctrl/Shift/Alt/Win 组合键
3. 配置文件持久化（快捷键=命令 格式）
4. 支持添加、删除、启用/禁用、列表查看
5. 绑定后自动执行系统命令

实现难度：中等

代替产品：AutoHotkey、xbindkeys、SharpKeys

---

110. set_window_opacity — 设置窗口透明度

功能简介：设置指定窗口的透明度级别（0–255）。

依赖库：Windows: -lgdi32 -luser32 -ldwmapi；Linux: -lX11 -lXcomposite -lXrender

亮点解析：

1. 跨平台透明度控制（Windows LayeredWindows / Linux _NET_WM_WINDOW_OPACITY）
2. 按标题/类名/PID 精准匹配窗口
3. 平滑渐变动画（自定义时长）
4. 批量处理或仅首个匹配窗口
5. 支持仅查询当前透明度

实现难度：中等

代替产品：WindowTop、Opacity、xprop (Linux)

---

111. install_font — 安装字体文件

功能简介：安装 TrueType/OpenType 字体文件到系统字体目录。

依赖库：Windows: -lgdi32 -luser32 -lshell32 -lshlwapi -lsetupapi；Linux: 无

亮点解析：

1. 跨平台字体安装（Windows AddFontResource / Linux fc-cache）
2. 支持 TTF/OTF/PCF/BDF/WOFF 等格式
3. 用户级和系统级两种安装模式
4. 自动解析字体名和样式（Regular/Bold/Italic）
5. 支持预览、卸载和批量安装

实现难度：中等

代替产品：FontForge、fc-cache、Windows 字体设置

---

112. charmap — 显示字符映射表

功能简介：显示 ASCII、扩展 ASCII 和 Unicode 字符编码映射表。

依赖库：无（完全自研）

亮点解析：

1. 支持 ASCII 表（0–127）和扩展 ASCII（128–255）
2. 支持 Unicode BMP 完整显示（0–65535）
3. 内置 30+ 种字符分类（控制符/数字/字母/CJK/箭头/符号等）
4. 支持按关键字搜索字符名称/分类/码点
5. 支持 CSV 和 HTML 格式导出

实现难度：简单

代替产品：Windows 字符映射表、GNOME Character Map、Unicode 字符表

---

113. ocr_image — OCR 文字识别

功能简介：从 BMP 图像文件中识别文字内容（基于模板匹配）。

依赖库：无（完全自研，需 -lm）

亮点解析：

1. 完整自研图像预处理（灰度转换、中值滤波、Otsu 二值化）
2. 基于垂直/水平投影的字符分割
3. 内置 40+ 个 5x7 点阵字符模板（数字/字母/标点）
4. 模板匹配识别 + 置信度评估
5. 支持自定义二值化阈值和去噪开关

实现难度：地狱

代替产品：Tesseract OCR、EasyOCR、PaddleOCR

---

114. denoise_image — 图像降噪处理

功能简介：对图像进行多种降噪处理（中值/高斯/双边/均值/非局部均值）。

依赖库：无（完全自研，需 -lm）

亮点解析：

1. 五种降噪算法（中值/高斯/双边/均值/非局部均值）
2. 完整 BMP 24 位图像 I/O
3. 双边滤波支持空间和颜色 sigma 调节
4. 非局部均值（NLM）支持滤波强度 h 调节
5. 支持迭代处理和批量处理

实现难度：中等

代替产品：OpenCV、GIMP、ImageMagick

---

115. color_balance — 色彩平衡调整

功能简介：调整图像的 RGB 色彩平衡、色温、饱和度，支持自动白平衡和直方图均衡化。

依赖库：无（完全自研，需 -lm）

亮点解析：

1. RGB 三通道独立增益和偏移控制
2. 色温调节（暖色/冷色，范围 -100–+100）
3. 饱和度控制（灰度到超饱和）
4. 灰度世界算法自动白平衡
5. 直方图均衡化增强对比度
6. 六种预设风格（暖色/冷色/复古/鲜艳/黑白/自动增强）

实现难度：简单

代替产品：Photoshop、GIMP、ImageMagick

---
🏆 最实用工具排行（前五）

基于日常开发、系统运维、数据处理等场景的使用频率和不可替代性综合评定。

🥇 第 1 名：search_in_files — 递归搜索文件内容

为什么最实用
替代 grep/findstr 的跨平台全文搜索工具，支持正则、大小写、扩展名过滤、排除目录、自动跳过超大文件。无论是代码审查、日志分析、配置文件定位，每天都会用到。

使用场景：代码搜索、日志分析、配置定位、敏感信息扫描

不可替代性：⭐⭐⭐⭐⭐

🥈 第 2 名：find_duplicates — 查找重复内容文件

为什么最实用
磁盘空间杀手级工具。通过大小预筛 + MD5/SHA1/SHA256 精确匹配，快速定位重复文件并清理，释放磁盘空间。支持试运行模式，安全可靠。

使用场景：磁盘清理、照片去重、备份去重、存储优化

不可替代性：⭐⭐⭐⭐⭐

🥉 第 3 名：auto_backup — 定时自动备份目录

为什么最实用
增量备份 + 时间戳版本管理 + 自动清理旧备份，一套组合拳解决数据安全问题。相比 rsync 更轻量，相比手动复制更智能。

使用场景：项目代码备份、配置文件保护、文档自动归档

不可替代性：⭐⭐⭐⭐

第 4 名：sync_folders — 同步两个目录

为什么最实用
四种同步方向（源→目标/目标→源/双向/镜像）+ 五种冲突策略，覆盖从简单复制到复杂同步的所有场景。支持 dry-run 预览，安全可靠。

使用场景：多设备文件同步、项目部署、备份恢复、镜像站点

不可替代性：⭐⭐⭐⭐

第 5 名：port_scan — 扫描端口开放状态

为什么最实用
轻量级 nmap 替代品，多线程并发扫描（最多 256 线程），内置 40+ 常见端口服务名映射，响应时间测量。安全审计和网络排障利器。

使用场景：安全审计、网络排障、服务发现、防火墙测试

不可替代性：⭐⭐⭐⭐

🔥 难度最大工具排行（前十）

基于算法复杂度、跨平台适配难度、代码量、领域知识要求综合评定。

🔥 第 1 名：style_transfer — 图片风格迁移

难度等级：地狱

为什么最难
完整自研 RGB ↔ LAB 色彩空间转换 + 多尺度纹理特征提取 + 多尺度金字塔架构 + 直方图匹配 + 纹理合成，涉及色彩科学、图像处理、信号处理多个领域。300+ 行纯 C，零依赖。

核心技术：LAB 色彩空间、直方图匹配、纹理合成、图像金字塔、局部方差

🔥 第 2 名：merge_audio_video — 合并音视频

难度等级：地狱

为什么最难
完整自研 WAV 和 AVI 双容器格式读写 + 音视频帧级精确同步 + PCM 格式互转 + 流式处理架构。涉及音频采样率匹配、视频帧率对齐、双流 AVI 封装。

核心技术：RIFF 容器、PCM 编码、帧同步算法、双流 AVI

🔥 第 3 名：ocr_image — OCR 文字识别

难度等级：地狱

为什么最难
完整自研图像预处理（灰度、中值滤波、Otsu 二值化）+ 字符分割（垂直/水平投影）+ 模板匹配识别 + 置信度评估。涉及计算机视觉、图像处理、模式识别。

核心技术：Otsu 算法、中值滤波、连通区域分析、模板匹配、置信度评估

🔥 第 4 名：packet_capture — 捕获网络数据包

难度等级：地狱

为什么最难
Linux AF_PACKET 原始套接字 + 完整解析以太网/IP/TCP/UDP/ICMP 头 + BPF 风格过滤器 + 五元组提取。涉及网络协议栈、数据包解析、字节序处理。

核心技术：原始套接字、协议解析、BPF 过滤、五元组提取

🔥 第 5 名：audio_equalizer — 音频均衡器

难度等级：复杂

为什么最难
完整 IIR 双二阶滤波器（Biquad）实现 + 10 段参量均衡 + 左右独立通道 + 8/16-bit PCM 支持。涉及数字信号处理、滤波器设计、音频编码。

核心技术：IIR 滤波器、Biquad 结构、参量均衡、PCM 编码

🔥 第 6 名：cache_service — 内存缓存服务

难度等级：复杂

为什么最难
完整 LRU/LFU/FIFO/TTL/混合五种淘汰策略 + RDB 快照 + AOF 日志 + 自研网络协议 + 一致性哈希 + 主从复制。涉及缓存算法、持久化、网络编程、分布式系统。

核心技术：LRU/LFU 算法、RDB/AOF 持久化、一致性哈希、主从复制

🔥 第 7 名：qr_generate — 生成二维码图片

难度等级：复杂

为什么最难
完整自研 QR 码编码器 + Reed-Solomon 纠错码 + 7 种版本 + 4 种纠错级别 + 自研 BMP 编码器。涉及编码理论、纠错码、图像编码。

核心技术：QR 编码、Reed-Solomon 纠错、BMP 编码、掩码生成

🔥 第 8 名：video_crop — 裁剪视频画面

难度等级：复杂

为什么最难
完全自研 AVI 容器解析与生成（RIFF/AVI 格式）+ 流式逐帧处理 + 完整索引重建 + 帧对齐处理。涉及多媒体容器格式、视频编解码基础、大文件 I/O。

核心技术：RIFF 容器、AVI 索引、帧对齐、流式处理

🔥 第 9 名：video_add_subtitle — 添加视频字幕

难度等级：复杂

为什么最难
完全自研 SRT 字幕解析器 + 5x7 点阵字体渲染引擎 + 精确时间同步 + 三种布局模式。涉及字幕格式解析、字体渲染、视频帧处理。

核心技术：SRT 解析、点阵渲染、时间轴同步、帧烧录

🔥 第 10 名：barcode_generate — 生成条形码

难度等级：复杂

为什么最难
完全自研 Code 128 编码逻辑（含起始符、数据编码、校验位计算、终止符）+ 内置 BMP 图像编码器 + 自定义渲染参数。涉及条码编码理论、校验算法、图像生成。

核心技术：Code 128 编码、模数校验、BMP 编码、条码渲染

📊 总结

排行 最实用工具（前五） 难度最大工具（前十）
1 search_in_files style_transfer
2 find_duplicates merge_audio_video
3 auto_backup ocr_image
4 sync_folders packet_capture
5 port_scan audio_equalizer
6 — cache_service
7 — qr_generate
8 — video_crop
9 — video_add_subtitle
10 — barcode_generate

---

📌 说明：

· 实用排行：基于实际开发、运维场景的使用频率和不可替代性。
· 难度排行：基于算法复杂度、跨平台适配难度、代码量、领域知识要求。